/**
给定一个非空整数数组，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现了一次的元素。
**/
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int result_num = nums[0];
		for(size_t i = 1; i < nums.size(); i++) {
			result_num = result_num ^ nums[i];
		}
        return result_num;
    }
};
